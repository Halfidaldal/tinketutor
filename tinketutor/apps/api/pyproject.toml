[project]
name = "synthesis-studio-api"
version = "0.1.0"
description = "Synthesis Studio — AI-powered educational workspace API"
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
